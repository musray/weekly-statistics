// First, import React
import React, {Component} from 'react';
import ReactDOM from 'react-dom';
// Then, import the pre-defined control form elements

import Button from './Button';


// 设置一个window.id，用于每一个任务的删除。
window.id = 0;

// class DayWrapper extends React.Component {
//     render() {
//         return (
//             ReactDOM.
//             <Button
//                 title={'删除'}
//                 controlFun={this.handleDeleteComponent}/>
//         )
//     }
// }

// export default DayWrapper;
export default DayControls;



